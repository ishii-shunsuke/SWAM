<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <a class="navbar-brand" href="#">Song with a memory</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-header" aria-controls="navbar-header" aria-expanded="false" aria-label="ナビゲーションの切替">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbar-header">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="#">トップ <span class="sr-only">(現位置)</span></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">マイページ</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">カート</a>
          </li>
        </ul>
      </div>
      <div class="float-right">
        <% if logged_in? %>
          <span class="guest"><%= full_name(current_user) %>さんこんにちは</span>  <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
        <% else %>
          <span class="guest">ゲストさんこんにちは</span>  <%= link_to "ログイン", new_user_session_path %><br>
          <span class="guest float-right">新規会員登録は <%= link_to "こちら", new_user_registration_path %>
        <% end %>
      </div>
    </nav>
</header>
